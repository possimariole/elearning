{{ form_start(form, {'attr': {'role': 'form'}}) }}
    <div class="row">
        <div class="col-6">
            <div class="form-group">
                {# Affichage du label du champ du formulaire. #}
                {{ form_label(form.libelle, "Libelle", {'attr': {'class': 'col-form-label', 'for':'libelle'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.libelle) }}

                {# Affichage du champ du formulaire. #}
                {{ form_widget(form.libelle, {'attr': {'class': 'form-control', 'libelle':'text', 'id': 'libelle'}}) }}
            </div>
        </div>
    </div>                        
    <button class="btn btn-xs btn-success float-right">
        <i class="fa fa-download"></i>&nbsp;
        {{ button_label|default('Enregistrer') }}
    </button>
    <a class="btn btn-xs btn-info float-right mr-2" href="{{ path('mode_paiement_index') }}"><i class="fa fa-list"></i>&nbsp;Liste</a>
    {{ form_rest(form) }}
{{ form_end(form) }}
{% extends 'base.html.twig' %}

{% block title %}Modifier un ModePaiement{% endblock %}

{% block body %}
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <h3 class="card-header text-uppercase ">Modifier un Mode de Paiement</h3>
                    <div class="card-body">
                        {{ include('mode_paiement/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                        {{ include('mode_paiement/_delete_form.html.twig') }}
                    </div>                            
                </div>
            </div>
        </div>
    </div>
{% endblock %}

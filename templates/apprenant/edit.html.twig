{% extends 'base.html.twig' %}

{% block title %}Modifier un Apprenant{% endblock %}

{% block body %}
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <h3 class="card-header text-uppercase ">Modifier un Apprenant</h3>
                    <div class="card-body">
                        {{ include('apprenant/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                        {{ include('apprenant/_delete_form.html.twig') }}
                    </div>                            
                </div>
            </div>
        </div>
    </div>
{% endblock %}

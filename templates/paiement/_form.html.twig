{{ form_start(form, {'attr': {'role': 'form'}}) }}
    <div class="row">
        <div class="col-6">
            <div class="form-group">
                {# Affichage du label du champ du formulaire. #}
                {{ form_label(form.somme, "Somme", {'attr': {'class': 'col-form-label', 'for':'somme'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.somme) }}

                {# Affichage du champ du formulaire. #}
                {{ form_widget(form.somme, {'attr': {'class': 'form-control', 'somme':'text', 'id': 'somme'}}) }}
            </div>
        </div>
         <div class="col-6">
            <div class="form-group">
            {# Affichage du label du champ du formulaire. #}
                {{ form_label(form.date, "Date", {'attr': {'class': 'col-form-label', 'for':'date'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.date) }}

                {# Affichage du champ du formulaire. #}
               {{ form_widget(form.date, {'attr': {'class': 'form-control', 'type':'date','id': 'date'}}) }}
             </div>
        </div>
    </div>

        <div class="row">
        <div class="col-6">
            <div class="form-group">
                {# Affichage du label du champ du formulaire. #}
                {{ form_label(form.inscription, "Inscription", {'attr': {'class': 'col-form-label', 'for':'inscription'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.inscription) }}

                {# Affichage du champ du formulaire. #}
                {{ form_widget(form.inscription, {'attr': {'class': 'form-control', 'inscription':'text', 'id': 'inscription'}}) }}
            </div>
        </div>
        <div class="col-6">
            <div class="form-group">
                {# Affichage du label du champ du formulaire. #}
                {{ form_label(form.modePaiement, "Mode de Paiement", {'attr': {'class': 'col-form-label', 'for':'modePaiement'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.modePaiement) }}

                {# Affichage du champ du formulaire. #}
                {{ form_widget(form.modePaiement, {'attr': {'class': 'form-control', 'modePaiement':'text', 'id': 'modePaiement'}}) }}
            </div>
        </div>


         </div> 
   
    <button class="btn btn-xs btn-success float-right">
        <i class="fa fa-download"></i>&nbsp;
        {{ button_label|default('Enregistrer') }}
    </button>
    <a class="btn btn-xs btn-info float-right mr-2" href="{{ path('paiement_index') }}"><i class="fa fa-list"></i>&nbsp;Liste</a>
    {{ form_rest(form) }}
{{ form_end(form) }}
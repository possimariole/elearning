{% extends 'base.html.twig' %}

{% block title %}Modifier un Paiement{% endblock %}

{% block body %}
     <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <h3 class="card-header text-uppercase ">Modifier un Paiement</h3>
                    <div class="card-body">
                        {{ include('paiement/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                        {{ include('paiement/_delete_form.html.twig') }}
                    </div>                            
                </div>
            </div>
        </div>
    </div>
{% endblock %}

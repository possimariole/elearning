{% extends 'base.html.twig' %}

{% block title %}Dispenser{% endblock %}

{% block body %}
    <h1>Dispenser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dispenser.id }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ dispenser.debut ? dispenser.debut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ dispenser.fin ? dispenser.fin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dispenser_index') }}">back to list</a>

    <a href="{{ path('dispenser_edit', {'id': dispenser.id}) }}">edit</a>

    {{ include('dispenser/_delete_form.html.twig') }}
{% endblock %}

{{ form_start(form, {'attr': {'role': 'form'}}) }}
    <div class="row">
        <div class="col-6">
            <div class="form-group">
                {# Affichage du label du champ du formulaire. #}
                {{ form_label(form.somme, "Somme", {'attr': {'class': 'col-form-label', 'for':'somme'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.somme) }}

                {# Affichage du champ du formulaire. #}
                {{ form_widget(form.somme, {'attr': {'class': 'form-control', 'somme':'text', 'id': 'somme'}}) }}
                </div>
        </div>
            <div class="col-6">
            <div class="form-group">
                {{ form_label(form.date, "Date", {'attr': {'class': 'col-form-label', 'for':'date'}}) }}
                {{ form_errors(form.date) }}
                {{ form_widget(form.date, {'attr': {'class': 'form-control', 'date':'text', 'id': 'date'}}) }}
            </div>
        </div>
        
    </div> 
         <div class="row">
        <div class="col-6">
            <div class="form-group">   
            {{ form_label(form.anneeAcademique, "AnneeAcademique", {'attr': {'class': 'col-form-label', 'for':'AnneeAcademique'}}) }}
                {{ form_errors(form.anneeAcademique) }}
                {{ form_widget(form.anneeAcademique, {'attr': {'class': 'form-control', 'date':'text', 'id': 'AnneeAcademique'}}) }}
             </div>
        </div>
        
    </div>                
    <button class="btn btn-xs btn-success float-right">
        <i class="fa fa-download"></i>&nbsp;
        {{ button_label|default('Enregistrer') }}
    </button>
    <a class="btn btn-xs btn-info float-right mr-2" href="{{ path('inscription_index') }}"><i class="fa fa-list"></i>&nbsp;Liste</a>
    {{ form_rest(form) }}
{{ form_end(form) }}

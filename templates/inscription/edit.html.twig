{% extends 'base.html.twig' %}

{% block title %}Edit Inscription{% endblock %}

{% block body %}
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <h3 class="card-header text-uppercase ">Modifier une inscription</h3>
                    <div class="card-body">
                        {{ include('inscription/_form.html.twig', {'button_label': 'Modifier'}) }}
                        {{ include('inscription/_delete_form.html.twig') }}
                    </div>                            
                </div>
            </div>
        </div>
    </div>
{% endblock %}
